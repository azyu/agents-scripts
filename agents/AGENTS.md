# AGENTS.md

- Write all agent prompts and bundled file contents in English.
- Structure each agent file around role, when to use, procedure, output format, and fallback behavior.
- Keep global rules in the root `AGENTS.md` and `rules/`; restate them here only when the agent needs a narrower variant.
- Prefer outputs that are easy to verify, such as findings with file references, concrete plans, or explicit checklists.
